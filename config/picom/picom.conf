# Backend
backend = "glx";
vsync = true;

# Rules
opacity-rule = [
	"90:class_g = 'kitty'"
];

#
active-opacity = 1
inactive-opacity = 0.80;

# 
shadow = true;
fading = true;
fade-delta = 5;
fade-in-step = 0.05;
fade-out-step = 0.05;
